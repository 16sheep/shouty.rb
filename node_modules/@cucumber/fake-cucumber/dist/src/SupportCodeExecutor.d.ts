import { Argument } from '@cucumber/cucumber-expressions';
import { messages } from '@cucumber/messages';
import IWorld from './IWorld';
import { AnyBody } from './types';
import ISupportCodeExecutor from './ISupportCodeExecutor';
export default class SupportCodeExecutor implements ISupportCodeExecutor {
    readonly stepDefinitionId: string;
    private readonly body;
    private readonly args;
    private readonly docString;
    private readonly dataTable;
    constructor(stepDefinitionId: string, body: AnyBody, args: ReadonlyArray<Argument<any>>, docString: messages.PickleStepArgument.IPickleDocString, dataTable: messages.PickleStepArgument.IPickleTable);
    execute(thisObj: IWorld): any;
    argsToMessages(): messages.TestCase.TestStep.StepMatchArgumentsList.IStepMatchArgument[];
}
