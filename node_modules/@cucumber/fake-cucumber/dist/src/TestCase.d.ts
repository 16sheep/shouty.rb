import ITestStep from './ITestStep';
import { EnvelopeListener } from './types';
import { messages } from '@cucumber/messages';
import IClock from './IClock';
import ITestCase from './ITestCase';
export default class TestCase implements ITestCase {
    readonly id: string;
    private readonly testSteps;
    private readonly pickleId;
    private readonly clock;
    constructor(id: string, testSteps: ITestStep[], pickleId: string, clock: IClock);
    toMessage(): messages.IEnvelope;
    execute(listener: EnvelopeListener, attempt: number, testCaseStartedId: string): Promise<void>;
}
