import { EnvelopeListener } from './types';
import SupportCode from './SupportCode';
import ITestPlan from './ITestPlan';
import ITestCase from './ITestCase';
export default class TestPlan implements ITestPlan {
    private readonly testCases;
    private readonly supportCode;
    constructor(testCases: ITestCase[], supportCode: SupportCode);
    execute(listener: EnvelopeListener): Promise<void>;
}
