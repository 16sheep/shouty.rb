import IHook from './IHook';
import { messages } from '@cucumber/messages';
import ISupportCodeExecutor from './ISupportCodeExecutor';
import { AnyBody } from './types';
export default class Hook implements IHook {
    readonly id: string;
    private readonly tagExpression;
    private readonly sourceReference;
    private readonly body;
    constructor(id: string, tagExpression: string | null, sourceReference: messages.ISourceReference, body: AnyBody);
    match(pickle: messages.IPickle): ISupportCodeExecutor | null;
    toMessage(): messages.IEnvelope;
    private matchesPickle;
}
