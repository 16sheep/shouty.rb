import ISupportCodeExecutor from './ISupportCodeExecutor';
import { Expression } from '@cucumber/cucumber-expressions';
import { messages } from '@cucumber/messages';
import IStepDefinition from './IStepDefinition';
import { AnyBody } from './types';
export default class ExpressionStepDefinition implements IStepDefinition {
    private readonly id;
    private readonly expression;
    private readonly sourceReference;
    private readonly body;
    constructor(id: string, expression: Expression, sourceReference: messages.ISourceReference, body: AnyBody);
    match(pickleStep: messages.Pickle.IPickleStep): ISupportCodeExecutor | null;
    toMessage(): messages.IEnvelope;
    private expressionType;
}
