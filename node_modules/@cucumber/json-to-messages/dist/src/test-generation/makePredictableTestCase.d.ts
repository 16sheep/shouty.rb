import { messages, IdGenerator } from '@cucumber/messages';
import { Query as GherkinQuery } from '@cucumber/gherkin';
import { MakeErrorMessage, IHook, IClock, ITestCase, IStepDefinition } from '@cucumber/fake-cucumber';
export default function makePredictableTestCase(pickle: messages.IPickle, stepDefinitions: ReadonlyArray<IStepDefinition>, beforeHooks: ReadonlyArray<IHook>, afterHooks: ReadonlyArray<IHook>, gherkinQuery: GherkinQuery, newId: IdGenerator.NewId, clock: IClock, makeErrorMessage: MakeErrorMessage): ITestCase;
