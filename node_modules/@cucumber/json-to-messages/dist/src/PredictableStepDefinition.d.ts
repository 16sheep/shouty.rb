import { IStepDefinition, ISupportCodeExecutor } from '@cucumber/fake-cucumber';
import { messages } from '@cucumber/messages';
export default class PredictableStepDefinition implements IStepDefinition {
    readonly id: string;
    private readonly stepId;
    private readonly location;
    readonly status: messages.TestStepFinished.TestStepResult.Status;
    readonly duration: number;
    readonly errorMessage?: string;
    constructor(id: string, stepId: string, location: string, status: messages.TestStepFinished.TestStepResult.Status, duration: number, errorMessage?: string);
    match(pickleStep: messages.Pickle.IPickleStep): ISupportCodeExecutor | null;
    toMessage(): messages.IEnvelope;
}
