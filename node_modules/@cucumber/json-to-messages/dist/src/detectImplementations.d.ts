/// <reference types="node" />
import { Readable } from 'stream';
import { Implementation } from './types';
export default function detectImplementations(jsonReadable: Readable): Promise<readonly Implementation[]>;
