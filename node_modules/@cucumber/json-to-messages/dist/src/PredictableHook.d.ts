import { IHook, ISupportCodeExecutor } from '@cucumber/fake-cucumber';
import { messages } from '@cucumber/messages';
export default class PredictableHook implements IHook {
    readonly id: string;
    private readonly scenarioId;
    private readonly location;
    readonly status: messages.TestStepFinished.TestStepResult.Status;
    readonly duration: number;
    readonly errorMessage?: string;
    constructor(id: string, scenarioId: string, location: string, status: messages.TestStepFinished.TestStepResult.Status, duration: number, errorMessage?: string);
    match(pickle: messages.IPickle): ISupportCodeExecutor;
    toMessage(): messages.IEnvelope;
}
