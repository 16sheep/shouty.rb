import { SupportCode } from '@cucumber/fake-cucumber';
import IPredictableSupportCode from './IPredictableSupportCode';
export default class PredictableSupportCode implements IPredictableSupportCode {
    private readonly supportCode;
    constructor(supportCode: SupportCode);
    addPredictableBeforeHook(location: string, scenarioId: string, status: string, duration?: number, stack?: string): void;
    addPredictableAfterHook(location: string, scenarioId: string, status: string, duration?: number, stack?: string): void;
    addPredictableStepDefinition(location: string, stepId: string, status: string, duration?: number, stack?: string): void;
    private statusFromString;
}
